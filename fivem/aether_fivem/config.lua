-- Aether FiveM bridge configuration (minimal).
-- Only the API key (secret) is required here — everything else comes
-- from the Aether dashboard (Dashboard > your server > FiveM).

Config = {}

-- Your API key from the Aether dashboard (FiveM module > Generate).
-- Treat this like a password.
Config.Secret = 'REPLACE_ME'
